
package com.autenticator.Database;

import com.autenticator.User;

public class UserDAOImpl implements UserDAO {

    @Override
    public void saveUser(User user) {
        // Implemente o código para salvar o usuário no banco de dados
    }

    @Override
    public User findByUsername(String username) {
        // Implemente o código para encontrar um usuário pelo nome de usuário no banco de dados
        return null;
    }
}